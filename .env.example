# Mot de passe webmaster (côté serveur uniquement)
# À définir sur Vercel : Settings > Environment Variables
WEBMASTER_PASSWORD=votre_mot_de_passe_secret

# Secret pour signer les tokens webmaster (obligatoire en production Vercel)
# En production, WEBMASTER_SECRET doit être défini, sinon la connexion webmaster échouera.
# WEBMASTER_SECRET=changez-moi-en-production

# Supabase - pour stocker les ressources ajoutées par le webmaster
# Créer un projet sur supabase.com, puis :
# 1. Exécuter supabase/schema.sql et supabase/schema-analytics.sql dans SQL Editor
# 2. Settings > API : copier URL et service_role key
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...

# Email : notification quand une proposition est reçue (optionnel, GRATUIT avec EmailJS)
# Solution EmailJS (gratuite, sans domaine requis) - CÔTÉ CLIENT :
# 1. Créer un compte gratuit sur https://www.emailjs.com/
# 2. Créer un service email (Gmail, Outlook, etc.)
# 3. Créer un template d'email avec les variables : {{name}}, {{url}}, {{description}}, {{category}}, {{subject}}, {{to_email}}
# 4. Récupérer Service ID, Template ID et Public Key depuis le dashboard EmailJS
# 5. Ajouter ces variables dans Vercel avec le préfixe VITE_ (pour Vite) :
#    - VITE_EMAILJS_SERVICE_ID=service_xxxxx
#    - VITE_EMAILJS_TEMPLATE_ID=template_xxxxx
#    - VITE_EMAILJS_PUBLIC_KEY=xxxxx
#    - VITE_NOTIFICATION_EMAIL=ressourcesmedge@gmail.com
# Note: Les variables VITE_ sont exposées côté client (c'est normal pour EmailJS)
